/**
 *
 * @param {array} target 被合并的目标数组
 * @param {array} src 合并的源数组
 * @param {string|object} key 合并的key值
 * @param {function} mergeFn 合并的规则函数
 * @return {array} 合并后的结果
 */
export function mergeBy(target: any[], src: any[], key: any, mergeFn: Function): any[];
/**
 *
 * @param {array} target 被合并的目标数组
 * @param {array} src 合并的源数组
 * @param {string|object} key 合并的key值
 * @param {function} mergeFn 合并的规则函数
 * @return {array} 合并后的结果
 */
export function mergeBy(target: any[], src: any[], key: any, mergeFn: Function): any[];
export function isSafeOrigin(origin: any): boolean;
export function isSafeOrigin(origin: any): boolean;
